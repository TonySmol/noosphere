{
  "name": "noosphere",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "pnpm build && npx gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^5.2.0",
    "typescript": "^5.4.0",
    "gh-pages": "^6.1.0"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2"
  }
}
